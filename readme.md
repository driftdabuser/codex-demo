# Codex Demo Project

* This project will be ued to demostrate github codex UI, CLI and IDE examples.

## Running the greeting

Run the module with Python and call the `greet` function from the interpreter:

```bash
python -c "from hello import greet; print(greet('World'))"
```
